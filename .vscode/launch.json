{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Program",
            "type": "node",
            "request": "launch",
            "args": ["-r", "ts-node/register", "${workspaceRoot}/src/main.ts"],
            "cwd": "${workspaceRoot}",
            "protocol": "inspector"
        }
    ]
}